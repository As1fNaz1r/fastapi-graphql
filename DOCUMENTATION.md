# 24 Jul
Added basic setup
BaseModel expects json from client side, annotates, validates it
Learned 405 Method not allowed, 422 unproccessable Entit error(empty json body, incorrect format)

# 26 Jul
Added Query parameters, path parameters
Put, Delete etc...

# 27 Jul
from typing import List
List is a generic alias
It’s shorthand for saying:
This is a container (list) that holds elements of a specific type.
List[T]
Where T is the type of items inside the list.